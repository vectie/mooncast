# Image generation provider port

Produces storyboard frames, character turnarounds, environments, props, and
cover variants from versioned approved references. The adapter returns model,
seed when available, source-asset digests, output hash, cost, rights terms,
safety result, and identity/continuity measurements.
